<section class="hero">
  <div class="hero-bg">
    <div class="eq-viz" aria-hidden="true">
      {Array.from({ length: 48 }).map((_, i) => (
        <span
          class="eq-bar"
          style={`--delay: ${i * 0.08}s; --height: ${0.2 + Math.random() * 0.8}; --h2: ${0.15 + Math.random() * 0.85}; --h3: ${0.1 + Math.random() * 0.9}; --h4: ${0.25 + Math.random() * 0.75}`}
        />
      ))}
    </div>
  </div>

  <div class="container hero-content">
    <div class="hero-badge">
      <span class="badge-dot" />
      Free &amp; open source
    </div>

    <h1 class="hero-title">
      <span class="title-line">What you're listening to,</span>
      <span class="title-line accent">now in your Teams status</span>
    </h1>

    <p class="hero-subtitle">
      A lightweight desktop app that watches your Spotify playback and
      automatically updates your Microsoft Teams status message. Set it,
      forget it, and let your team know what's playing.
    </p>

    <div class="hero-actions">
      <a href="/spotify_nowplaying/download" class="btn btn-primary">
        <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="M21 15v4a2 2 0 01-2 2H5a2 2 0 01-2-2v-4"/><polyline points="7 10 12 15 17 10"/><line x1="12" y1="15" x2="12" y2="3"/></svg>
        Download
      </a>
      <a href="/spotify_nowplaying/docs" class="btn btn-ghost">
        Read the docs
        <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round"><line x1="5" y1="12" x2="19" y2="12"/><polyline points="12 5 19 12 12 19"/></svg>
      </a>
    </div>

    <div class="hero-platforms">
      <span class="platform"><svg width="14" height="14" viewBox="0 0 24 24" fill="currentColor"><path d="M0 3.449L9.75 2.1v9.45H0m10.949-9.6L24 0v11.4H10.949M0 12.6h9.75v9.45L0 20.55M10.949 12.6H24V24l-12.9-1.8"/></svg> Windows</span>
      <span class="platform"><svg width="14" height="14" viewBox="0 0 24 24" fill="currentColor"><path d="M18.71 19.5c-.83 1.24-1.71 2.45-3.05 2.47-1.34.03-1.77-.79-3.29-.79-1.53 0-2 .77-3.27.82-1.31.05-2.3-1.32-3.14-2.53C4.25 17 2.94 12.45 4.7 9.39c.87-1.52 2.43-2.48 4.12-2.51 1.28-.02 2.5.87 3.29.87.78 0 2.26-1.07 3.82-.91.65.03 2.47.26 3.64 1.98-.09.06-2.17 1.28-2.15 3.81.03 3.02 2.65 4.03 2.68 4.04-.03.07-.42 1.44-1.38 2.83M13 3.5c.73-.88 1.94-1.46 3.03-1.5.15 1.61-.44 3.22-1.32 4.36-.86 1.1-2.27 1.96-3.66 1.84-.17-1.58.5-3.18 1.95-4.7"/></svg> macOS</span>
      <span class="platform"><svg width="14" height="14" viewBox="0 0 24 24" fill="currentColor"><path d="M20.64 12.2c-.09 1.62.67 2.39.71 2.41-.06.2-.46 1.6-1.53 3.15-.97 1.42-1.99 2.84-3.58 2.87-1.56.03-2.07-1-3.86-1-1.79 0-2.35.97-3.83 1.03-1.54.06-2.7-1.53-3.69-2.95C2.8 15.28 2.29 11.36 3.9 8.68c.8-1.38 2.23-2.26 3.78-2.28 1.48-.03 2.88 1 3.78 1 1.09 0 3.14-1.41 5.29-1.2.9.02 3.43.36 5.05 2.74-.13.08-2.98 1.72-2.95 5.05M15.96 4.1c.66-.8 1.86-1.34 2.85-1.47.13 1.73-.5 3.47-1.37 4.63-.84 1.11-2.22 1.98-3.59 1.85-.15-1.63.6-3.23 2.11-5.01"/></svg> Linux</span>
    </div>
  </div>
</section>

<style>
  .hero {
    position: relative;
    padding: 8rem 0 6rem;
    overflow: hidden;
  }

  .hero-bg {
    position: absolute;
    inset: 0;
    pointer-events: none;
  }

  .eq-viz {
    display: flex;
    align-items: flex-end;
    justify-content: center;
    gap: 3px;
    height: 100%;
    padding: 0 2rem;
    mask-image: linear-gradient(to top, transparent 20%, rgba(0,0,0,0.5) 50%, transparent 85%);
    -webkit-mask-image: linear-gradient(to top, transparent 20%, rgba(0,0,0,0.5) 50%, transparent 85%);
  }

  .eq-bar {
    width: 8px;
    background: linear-gradient(to top, var(--accent), rgba(29, 185, 84, 0.2));
    border-radius: 4px 4px 1px 1px;
    opacity: 0.12;
    height: calc(var(--height) * 350px);
    animation: eq-bounce 1.6s ease-in-out infinite;
    animation-delay: var(--delay);
  }

  @keyframes eq-bounce {
    0%, 100% { height: calc(var(--height) * 350px); }
    33% { height: calc(var(--h2) * 350px); }
    66% { height: calc(var(--h3) * 350px); }
  }

  .hero-content {
    position: relative;
    z-index: 2;
    display: flex;
    flex-direction: column;
    align-items: center;
    text-align: center;
    gap: 1.75rem;
  }

  .hero-badge {
    display: inline-flex;
    align-items: center;
    gap: 0.5rem;
    padding: 0.35rem 1rem;
    border-radius: 999px;
    border: 1px solid var(--border-medium);
    background: var(--bg-surface);
    font-size: 0.8rem;
    font-weight: 500;
    color: var(--text-secondary);
    animation: fade-in-up 0.6s ease-out;
  }

  .badge-dot {
    width: 7px;
    height: 7px;
    border-radius: 50%;
    background: var(--accent);
    box-shadow: 0 0 8px var(--accent-glow);
  }

  .hero-title {
    display: flex;
    flex-direction: column;
    gap: 0.25rem;
    font-family: var(--font-display);
    font-weight: 900;
    animation: fade-in-up 0.6s ease-out 0.1s both;
  }

  .title-line {
    display: block;
    color: var(--text-primary);
    font-size: clamp(2.5rem, 6vw, 4.5rem);
    line-height: 1.1;
    letter-spacing: -0.03em;
    white-space: nowrap;
  }

  .title-line.accent {
    background: linear-gradient(135deg, var(--accent) 0%, #26e07a 50%, #7fd1a4 100%);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
  }

  .hero-subtitle {
    max-width: 580px;
    font-size: 1.1rem;
    color: var(--text-secondary);
    line-height: 1.7;
    animation: fade-in-up 0.6s ease-out 0.2s both;
  }

  .hero-actions {
    display: flex;
    gap: 0.75rem;
    animation: fade-in-up 0.6s ease-out 0.3s both;
  }

  .btn {
    display: inline-flex;
    align-items: center;
    gap: 0.5rem;
    padding: 0.75rem 1.5rem;
    border-radius: 12px;
    font-family: var(--font-display);
    font-weight: 500;
    font-size: 0.9rem;
    letter-spacing: -0.01em;
    transition: all 0.25s;
    cursor: pointer;
    border: none;
  }

  .btn-primary {
    background: var(--accent);
    color: var(--bg-deep);
    box-shadow: 0 0 30px var(--accent-glow);
  }

  .btn-primary:hover {
    transform: translateY(-2px);
    box-shadow: 0 4px 40px var(--accent-glow);
  }

  .btn-ghost {
    background: var(--bg-elevated);
    color: var(--text-primary);
    border: 1px solid var(--border-medium);
  }

  .btn-ghost:hover {
    background: var(--bg-hover);
    border-color: var(--text-muted);
  }

  .hero-platforms {
    display: flex;
    gap: 1.5rem;
    animation: fade-in-up 0.6s ease-out 0.4s both;
  }

  .platform {
    display: flex;
    align-items: center;
    gap: 0.4rem;
    font-size: 0.8rem;
    color: var(--text-muted);
    opacity: 0.7;
  }

  @media (max-width: 640px) {
    .hero {
      padding: 5rem 0 3.5rem;
    }
    .hero-actions {
      flex-direction: column;
    }
    .eq-viz {
      gap: 1px;
    }
    .eq-bar {
      width: 4px;
    }
    .title-line {
      white-space: normal;
    }
  }
</style>
